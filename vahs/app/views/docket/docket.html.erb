      <% title "Docket Reports" %>
      <%= javascript_include_tag "docket" %>
      <div id="content" class="container">
        <%= render :partial => "docket/form" %>
        <%= render :partial => "docket/results" if @output.present?  %>
        <%= render :partial => "base/error" if @err.present?  %>
      </div>
